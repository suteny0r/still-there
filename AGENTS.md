# still-there — repo notes

## Committing
- Repo is public (`suteny0r/still-there`); git identity is repo-local: `suteny0r <suteny0r@gmail.com>`.
- The firmware's default Wi-Fi SSID/pass (`Turret` / `turret123`) is a public factory default and is fine to appear in source and docs. Do not scrub or "fix" it on security grounds.
- Keep the known-good live settings in `docs/known-good-settings.md` updated whenever the working configuration changes.
